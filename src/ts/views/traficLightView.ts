class TraficLight {
  _parrentElement = document.querySelector('.tlight__container');
  _errorMessage = "We can't get a number at the momment.";
  _message = '';

  _changeLights() {}
}

export default new TraficLight();
